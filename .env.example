# Runtime configuration. Copy to .env.production on the deployment host.
DOMAIN=www.leonstrotz.com

# Fine-grained GitHub token with read-only access to Merkelmore repositories.
# Enables live inventory of private repositories. Leave unset for public-only metadata.
GITHUB_READ_TOKEN=
